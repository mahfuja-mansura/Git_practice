package Linked_list.Singly_linked_list.Lab_ques_prac;

public class Main 
{
    public static void main(String[]args)
    {
        
    }
    
   /*  public static void rearrangePassengers(Node head)
    {
        Node temp = head;
        Node newNode = null;

        while(temp!=null && temp.next!=null)
        {
            if(temp.elem>temp.next.elem)
            {
                newNode.elem = temp.next.elem;
                temp.next = temp;
                temp = newNode;
            }
            temp = temp.next;
        }
    }*/
}
